{
	"type": "module",
	"dependencies": {
		"three": "^0.167.1",
		"vite": "^5.4.1"
	},
	"scripts": {
		"build": "pnpx vite build",
		"deploy": "pnpm run build && pnpx gh-pages -d dist",
		"dev": "pnpx vite dev"
	},
	"devDependencies": {
		"gh-pages": "^6.1.1"
	}
}
